{
  "name": "helixbench-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare-assets": "node scripts/prepare-assets.js",
    "dev": "node scripts/prepare-assets.js && wrangler dev",
    "deploy": "node scripts/prepare-assets.js && wrangler deploy",
    "kv:create": "wrangler kv namespace create HELIX_CACHE",
    "kv:create-preview": "wrangler kv namespace create HELIX_CACHE --preview"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
